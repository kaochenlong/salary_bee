<div class="min-h-screen bg-gradient-to-br from-yellow-100 to-yellow-200 p-4">
  <div class="max-w-2xl mx-auto">

    <div class="brutalist-card p-8">
      <div class="text-center mb-8">
        <h1 class="text-4xl font-black text-black mb-2 brutalist-tilt-minus-1">
          新增公司
        </h1>
        <p class="text-lg font-bold text-black">建立您的公司資料</p>
      </div>

      <!-- 錯誤訊息 -->
      <% if @company.errors.any? %>
        <div class="brutalist-error p-4 mb-6">
          <h3 class="font-bold mb-2">請修正以下錯誤：</h3>
          <ul class="list-disc list-inside">
            <% @company.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with model: @company, local: true, class: "space-y-6" do |form| %>

        <div>
          <%= form.label :name, "公司名稱", class: "block text-black font-bold mb-2 text-lg" %>
          <%= form.text_field :name,
              required: true,
              autofocus: true,
              placeholder: "請輸入公司名稱",
              class: "brutalist-input w-full px-4 py-4" %>
        </div>

        <div>
          <%= form.label :description, "公司簡介", class: "block text-black font-bold mb-2 text-lg" %>
          <%= form.text_area :description,
              rows: 4,
              placeholder: "請簡單介紹您的公司...",
              class: "brutalist-input w-full px-4 py-4 resize-none" %>
          <div class="text-sm text-black mt-1 font-medium">
            選填項目，可以簡單描述公司業務或特色
          </div>
        </div>

        <div class="pt-4 space-y-3">
          <%= form.submit "建立公司",
              class: "brutalist-button-primary w-full py-4 px-6 text-xl" %>

          <%= link_to "取消", dashboard_path,
              class: "brutalist-button-secondary block w-full py-3 px-6 text-center text-lg" %>
        </div>

      <% end %>

      <!-- 提示資訊 -->
      <div class="mt-8 bg-white border-4 border-black p-4 neubrutalist-shadow brutalist-tilt-1">
        <h3 class="font-bold text-black mb-2">💡 小提示</h3>
        <p class="text-black font-medium text-sm">
          建立公司後，您就可以開始新增員工資料並管理薪資了。
          公司名稱建立後仍可修改，請放心填寫。
        </p>
      </div>
    </div>
  </div>

  <!-- 背景裝飾 -->
  <div class="fixed inset-0 pointer-events-none overflow-hidden">
    <div class="absolute top-32 left-10 w-6 h-6 bg-green-500 border-4 border-black neubrutalist-shadow brutalist-tilt-1"></div>
    <div class="absolute bottom-20 right-20 w-4 h-4 bg-blue-600 border-4 border-black neubrutalist-shadow brutalist-tilt-minus-2"></div>
  </div>
</div>